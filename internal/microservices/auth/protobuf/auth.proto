syntax = "proto3";

package auth;

option go_package = "github.com/go-park-mail-ru/2022_2_VDonate/internal/microservices/auth/protobuf";


message SessionID {
  string session_id = 1;
}

message UserSession {
  string session_id = 1;
  uint64 user_id = 2;
}

service AuthService {
//  rpc IsSameSession(UserSession) returns(common.Bool);
}